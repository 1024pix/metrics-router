
location /grafana {
  proxy_set_header Host <%= ENV["GRAFANA_URL"] %>;
  proxy_redirect <%= ENV["GRAFANA_URL"] %> https://metrics.integration.pix.fr/grafana;
  proxy_pass <%= ENV["GRAFANA_URL"] %>;
}

location /db {
  proxy_pass <%= ENV["INFLUX_DB_URL"] %>;
}